# 🌳 إعادة بناء شجرة ثنائية من بيانات الاجتياز (Inorder و Postorder)

هذا المستودع يحتوي على حل احترافي وخوارزمي لمسألة شائعة في هياكل البيانات والخوارزميات: إعادة بناء شجرة ثنائية بالكامل باستخدام مصفوفتي اجتياز **Inorder** و **Postorder** لها.

## 🎯 وصف المسألة

تُعطى مصفوفتان:
1.  **`inorder` (التناظري):** اجتياز يسار -> جذر -> يمين.
2.  **`postorder` (اللاحق):** اجتياز يسار -> يمين -> جذر.

الهدف هو إنشاء الشجرة الثنائية الأصلية بالكامل وإرجاع كائن الجذر الخاص بها.

## 💡 المنطق والخوارزمية

تم استخدام خوارزمية **تكرارية (Recursion)** تعتمد على الخصائص المميزة لـ Postorder:

1.  **تحديد الجذر:** القيمة الأخيرة في `postorder` هي دائماً **جذر** الشجرة الفرعية الحالية.
2.  **التقسيم في Inorder:** بمجرد تحديد الجذر، يتم استخدامه لتقسيم مصفوفة `inorder` إلى جزئين:
    * القيم التي على **يسار** الجذر هي الشجرة الفرعية **اليسرى**.
    * القيم التي على **يمين** الجذر هي الشجرة الفرعية **اليمنى**.
3.  **تحديد النطاقات:** يتم استخدام طول الأجزاء اليسرى واليمنى من `inorder` لتقسيم نطاق `postorder` المقابل أيضاً.
4.  **التحسين:** لضمان تعقيد زمني مثالي، تم استخدام **خريطة تجزئة (Hash Map)** لتخزين مؤشرات قيم `inorder`. هذا يسمح بتحديد موضع الجذر في `inorder` في وقت ثابت ($O(1)$) بدلاً من البحث الخطي ($O(N)$).

## 🚀 تحليل التعقيد

| المعيار | التعقيد |
| :--- | :--- |
| **التعقيد الزمني (Time Complexity)** | $O(N)$ |
| **التعقيد المكاني (Space Complexity)** | $O(N)$ |

حيث $N$ هو عدد العقد في الشجرة.

